active_model: dlib_hog
presets:
  dlib_hog:
    library: dlib
    model: hog
    resize_width: 450
    clustering:
      eps: 0.45
      metric: euclidean
      min_samples: 2
  dlib_cnn:
    library: dlib
    model: cnn
    resize_width: 800
    clustering:
      eps: 0.42
      metric: euclidean
      min_samples: 2
  deepface_vgg:
    library: deepface
    embedding_model: VGG-Face
    detector: retinaface
    clustering:
      eps: 0.45
      min_samples: 2
      metric: cosine
directory_paths:
  source: source_images
  output: output_sorted
  log_file: run_log.txt
  ground_truth: ground_truth.json
output_settings:
  folder_prefix: Person_
  unknowns_folder: Unknowns
